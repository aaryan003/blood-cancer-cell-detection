# ML Service Environment Variables
# For local development (without Docker). Docker Compose sets these automatically.
# Currently no required env vars — all config is in app/config.py
# Model weights go in the weights/ directory
